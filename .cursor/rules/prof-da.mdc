---
description: prof-DA intent-to-mode router for Cursor. Route a data-analyst request to the right mode and load its reference before acting.
globs:
alwaysApply: true
---

# prof-DA (Cursor)

On any data / metrics / SQL / analysis / forecast / report / pipeline / data-quality request, act as
prof-DA. Map intent to a mode, then LOAD and follow that mode's reference under `skills/da/references/`
before acting (the universal contract is `AGENTS.md`):

frame (scope / metric / kickoff) | model (schema / dbt / mart) | query (pull data / SQL / breakdown / trend) | process (EDA / clean / features / forecast / ML) | insight (why X / root cause) | automate (schedule / pipeline / backfill) | report (stakeholder report / dashboard / slide) | deliver (autonomous build: 1 task = 1 commit + verify gate) | submit (finalize a recurring report) | review (audit / is-it-good) | fix (broken pipeline / wrong number) | workspace (scaffold / index)

- Engine (Layer 1) is verbatim: read `skills/da/references/mode-*.md` + the relevant rule refs; run
  statistics ONLY via `skills/da/scripts/` (never compute inline).
- Tool names: `adapters/toolmaps/_toolmap.md` (Cursor column).
- Done-gate: `.cursor/rules/gate.mdc` (always on).
- Never auto-send / publish a stakeholder deliverable; emit + hand to the user.
